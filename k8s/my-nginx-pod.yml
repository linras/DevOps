apiVersion: v1
kind: Pod
metadata:
  name: my-nginx-pod
  labels:
    component: web
    name: my-app

spec:
  containers:
    - name: my-nginx
      image: nginx:alpine #linras/mynginx-k8s:latest
      ports:
        - containerPort: 80